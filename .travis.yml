sudo: false
language: python
python:
  - "2.7"
before_install:
  - pip install codecov
after_success:
  - codecov
install:
  - pip install -Ur requirements-dev.txt
script:
  - flake8
  - tox --recreate
